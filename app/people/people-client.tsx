"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";

type Profile = { id: string; username: string | null; display_name: string | null; avatar_url: string | null; bio: string | null; country: string | null; language: string | null };
type Relationship = { id: string; requester_id: string; addressee_id: string; status: string; created_at: string; updated_at: string };
type NearbyPerson = { user_id: string; display_name: string | null; avatar_url: string | null; country_code: string | null; region: string | null; city: string | null; distance_bucket: string };

export default function PeopleClient({ currentUserId, profiles, relationships: initialRelationships, backendReady, backendMessage }: { currentUserId: string; profiles: Profile[]; relationships: Relationship[]; backendReady: boolean; backendMessage: string | null }) {
  const router = useRouter(); const supabase = useMemo(() => createClient(), []);
  const [relationships, setRelationships] = useState(initialRelationships); const [nearby, setNearby] = useState<NearbyPerson[]>([]); const [nearbyEnabled, setNearbyEnabled] = useState(false); const [blocked, setBlocked] = useState<Set<string>>(new Set()); const [busy, setBusy] = useState<string | null>(null); const [notice, setNotice] = useState<string | null>(null);
  const relationByUser = useMemo(() => { const map = new Map<string, Relationship>(); relationships.forEach((r) => map.set(r.requester_id === currentUserId ? r.addressee_id : r.requester_id, r)); return map; }, [relationships, currentUserId]);
  async function refreshRelationships(){const r=await fetch("/api/people/relationships",{cache:"no-store"});const j=await r.json();if(r.ok&&j.relationships)setRelationships(j.relationships)}
  async function refreshBlocks(){const r=await fetch("/api/people/blocks",{cache:"no-store"});const j=await r.json();if(r.ok)setBlocked(new Set((j.blocks??[]).map((b:{blocked_id:string})=>b.blocked_id)))}
  useEffect(()=>{void refreshBlocks()},[]);
  async function requestConnection(userId:string){setBusy(userId);setNotice(null);const r=await fetch("/api/people/relationships",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({addresseeId:userId})});const j=await r.json();setBusy(null);if(!r.ok)return setNotice(j.detail||j.error||"Η αίτηση απέτυχε.");await refreshRelationships()}
  async function respond(relationshipId:string,action:"accept"|"decline"){setBusy(relationshipId);setNotice(null);const r=await fetch("/api/people/relationships",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({relationshipId,action})});const j=await r.json();setBusy(null);if(!r.ok)return setNotice(j.detail||j.error||"Η ενέργεια απέτυχε.");await refreshRelationships()}
  async function openConversation(otherUserId:string){setBusy(otherUserId);setNotice(null);const r=await fetch("/api/messages/conversations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({otherUserId})});const j=await r.json();setBusy(null);if(!r.ok)return setNotice(j.detail||j.error||"Δεν άνοιξε η συνομιλία.");router.push(`/messages/${j.conversationId}`)}
  async function toggleBlock(userId:string){const isBlocked=blocked.has(userId);if(!isBlocked&&!window.confirm("Να αποκλειστεί αυτός ο χρήστης; Δεν θα μπορεί να δημιουργήσει νέα σχέση ή επικοινωνία μαζί σου."))return;setBusy(`block:${userId}`);setNotice(null);const r=await fetch("/api/people/blocks",{method:isBlocked?"DELETE":"POST",headers:{"content-type":"application/json"},body:JSON.stringify({blockedId:userId})});const j=await r.json();setBusy(null);if(!r.ok)return setNotice(j.detail||j.error||"Η αλλαγή αποκλεισμού απέτυχε.");await refreshBlocks();await refreshRelationships();setNotice(isBlocked?"Ο αποκλεισμός αφαιρέθηκε.":"Ο χρήστης αποκλείστηκε.")}
  async function findNearby(){setNotice(null);if(!navigator.geolocation)return setNotice("Η συσκευή δεν υποστηρίζει γεωεντοπισμό.");setBusy("nearby");navigator.geolocation.getCurrentPosition(async(position)=>{const{latitude,longitude,accuracy}=position.coords;const{error:pe}=await supabase.rpc("pantavion_update_location_presence",{p_latitude:latitude,p_longitude:longitude,p_accuracy_meters:accuracy,p_nearby_enabled:true,p_expires_minutes:30});if(pe){setBusy(null);return setNotice(pe.message)}const{data,error}=await supabase.rpc("pantavion_find_nearby_people",{p_radius_meters:25000,p_limit:50});setBusy(null);if(error)return setNotice(error.message);setNearby((data??[]) as NearbyPerson[]);setNearbyEnabled(true);setNotice(data?.length?`Βρέθηκαν ${data.length} άτομα στην επιλεγμένη ακτίνα.`:"Δεν βρέθηκαν ακόμη διαθέσιμα άτομα κοντά σου.")},(e)=>{setBusy(null);setNotice(e.message||"Δεν δόθηκε πρόσβαση στην τοποθεσία.")},{enableHighAccuracy:true,timeout:10000,maximumAge:60000})}
  async function disableNearby(){setBusy("nearby");setNotice(null);const{error}=await supabase.rpc("pantavion_disable_location_presence");setBusy(null);if(error)return setNotice(error.message);setNearby([]);setNearbyEnabled(false);setNotice("Η παρουσία Nearby απενεργοποιήθηκε.")}
  return <main className="min-h-screen bg-[#f5f9fd] text-slate-950"><section className="mx-auto max-w-6xl px-4 py-6 sm:px-8 lg:px-12"><nav className="flex items-center justify-between rounded-2xl border border-slate-200 bg-white px-4 py-3 shadow-sm"><Link href="/" className="font-black tracking-[0.18em] text-[#173f72] no-underline">PANTAVION</Link><div className="flex gap-2"><Link href="/ecosystem" className="rounded-full border border-slate-200 px-3 py-2 text-xs font-black text-slate-600 no-underline">Ecosystem</Link><Link href="/profile" className="rounded-full bg-[#2467aa] px-3 py-2 text-xs font-black text-white no-underline">Profile</Link></div></nav><header className="py-9"><p className="text-[10px] font-black uppercase tracking-[0.26em] text-[#3474b8]">PEOPLE & SOCIAL</p><h1 className="mt-2 text-4xl font-black tracking-[-0.04em] text-[#173f72] sm:text-5xl">Άνθρωποι, αιτήματα, πραγματικές σχέσεις.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-600 sm:text-base">Profile → request → accept → conversation → message, με privacy και block controls.</p></header>{!backendReady&&<div className="mb-5 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800"><strong>Backend migration pending.</strong>{backendMessage?<span className="mt-1 block text-xs opacity-70">{backendMessage}</span>:null}</div>}{notice&&<div className="mb-5 rounded-2xl border border-slate-200 bg-white p-4 text-sm font-semibold text-slate-700">{notice}</div>}<section className="mb-6 rounded-[1.35rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-[0.18em] text-[#3474b8]">NEARBY PEOPLE</p><h2 className="mt-1 text-xl font-black text-[#173f72]">Άτομα κοντά σου, χωρίς αποκάλυψη ακριβούς θέσης.</h2></div><div className="flex gap-2"><button disabled={busy==="nearby"} onClick={findNearby} className="rounded-full bg-[#2467aa] px-4 py-2.5 text-xs font-black text-white disabled:opacity-40">{busy==="nearby"?"Εντοπισμός…":"Βρες κοντινά άτομα"}</button>{nearbyEnabled&&<button onClick={disableNearby} className="rounded-full border border-slate-200 px-4 py-2.5 text-xs font-black text-slate-600">Κλείσιμο Nearby</button>}</div></div>{nearby.length>0&&<div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{nearby.map(p=><article key={p.user_id} className="rounded-2xl border border-slate-100 bg-slate-50 p-4"><div className="flex items-center justify-between"><div><p className="font-black">{p.display_name||"Pantavion User"}</p><p className="text-xs text-slate-500">{[p.city,p.region,p.country_code].filter(Boolean).join(" · ")}</p></div><span className="rounded-full bg-white px-2.5 py-1 text-[10px] font-black text-[#2467aa]">{p.distance_bucket}</span></div></article>)}</div>}</section><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{profiles.map(profile=>{const relation=relationByUser.get(profile.id),incoming=relation?.status==="pending"&&relation.addressee_id===currentUserId,outgoing=relation?.status==="pending"&&relation.requester_id===currentUserId,connected=relation?.status==="accepted",isBlocked=blocked.has(profile.id);return <article key={profile.id} className="rounded-[1.35rem] border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-full bg-blue-50 text-sm font-black text-blue-700">{(profile.display_name||profile.username||"P").slice(0,1).toUpperCase()}</div><div><h2 className="font-black">{profile.display_name||profile.username||"Pantavion User"}</h2><p className="text-xs text-slate-500">{[profile.country,profile.language].filter(Boolean).join(" · ")||"Pantavion"}</p></div></div>{profile.bio&&<p className="mt-3 line-clamp-2 text-sm text-slate-600">{profile.bio}</p>}<div className="mt-4 space-y-2">{!isBlocked&&(!relation||["declined","removed"].includes(relation.status))&&<button disabled={!backendReady||busy===profile.id} onClick={()=>requestConnection(profile.id)} className="w-full rounded-full bg-[#2467aa] px-4 py-2.5 text-sm font-black text-white disabled:opacity-40">Αίτημα σύνδεσης</button>}{!isBlocked&&outgoing&&<div className="rounded-full border border-amber-200 bg-amber-50 px-4 py-2.5 text-center text-sm font-black text-amber-700">Αναμονή αποδοχής</div>}{!isBlocked&&incoming&&<div className="flex gap-2"><button onClick={()=>respond(relation!.id,"accept")} className="flex-1 rounded-full bg-emerald-600 px-3 py-2.5 text-xs font-black text-white">Αποδοχή</button><button onClick={()=>respond(relation!.id,"decline")} className="rounded-full border px-3 py-2.5 text-xs font-black">Όχι</button></div>}{!isBlocked&&connected&&<button onClick={()=>openConversation(profile.id)} className="w-full rounded-full bg-[#123b67] px-4 py-2.5 text-sm font-black text-white">Μήνυμα</button>}<button disabled={busy===`block:${profile.id}`} onClick={()=>toggleBlock(profile.id)} className="w-full rounded-full border border-slate-200 px-4 py-2 text-xs font-black text-slate-500 disabled:opacity-40">{isBlocked?"Άρση αποκλεισμού":"Αποκλεισμός"}</button></div></article>})}</div>{!profiles.length&&<div className="rounded-[1.35rem] border bg-white p-6 text-sm text-slate-600">Δεν υπάρχουν ακόμη ορατά profiles για αυτόν τον λογαριασμό.</div>}</section></main>
