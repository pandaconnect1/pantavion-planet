import { NextResponse } from "next/server";
import { compileSovereignExecutionManifest } from "@/core/sovereign/sovereign-execution-manifest";
import { requireFounderIdentity } from "@/lib/owner-control/decision-queue";
import { createClient } from "@/lib/supabase/server";
export const dynamic = "force-dynamic"; export const revalidate = 0; const MAX_REQUEST_BYTES=32_768;
function json(body:unknown,status:number){return NextResponse.json(body,{status,headers:{"Cache-Control":"no-store, max-age=0","X-Content-Type-Options":"nosniff"}})}
export async function POST(request:Request){const supabase=await createClient();const {data:auth,error}=await supabase.auth.getUser();if(error||!auth.user)return json({ok:false,error:"authentication_required"},401);try{requireFounderIdentity(auth.user.id)}catch{return json({ok:false,error:"founder_only"},403)}const {data:assurance,error:aalError}=await supabase.auth.mfa.getAuthenticatorAssuranceLevel();if(aalError||assurance?.currentLevel!=="aal2")return json({ok:false,error:"aal2_required"},403);if(!request.headers.get("content-type")?.toLowerCase().startsWith("application/json"))return json({ok:false,error:"application_json_required"},415);const raw=await request.text().catch(()=>null);if(raw===null)return json({ok:false,error:"request_body_unreadable"},400);if(Buffer.byteLength(raw,"utf8")>MAX_REQUEST_BYTES)return json({ok:false,error:"request_too_large"},413);let input:unknown;try{input=JSON.parse(raw)}catch{return json({ok:false,error:"invalid_json"},400)}try{return json({ok:true,requesterUserId:auth.user.id,...compileSovereignExecutionManifest(input)},200)}catch(e){const detail=e instanceof Error&&e.message.startsWith("invalid_execution_manifest:")?e.message:"invalid_execution_manifest";return json({ok:false,error:"invalid_execution_manifest",detail},400)}}
