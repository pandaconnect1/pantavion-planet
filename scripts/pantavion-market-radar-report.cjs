const fs = require("fs");
const path = require("path");

const outDir = path.join(process.cwd(), "data", "runtime-reports");
fs.mkdirSync(outDir, { recursive: true });

const report = {
  id: "pantavion_cloud_market_radar_report_v1",
  generatedAt: new Date().toISOString(),
  status: "scheduled_cloud_report",
  truth: "This report is generated by GitHub Actions schedule when repository workflow is enabled. Deeper live web research needs provider/search API credentials.",
  watchedDomains: [
    "AI models",
    "agent protocols",
    "voice and translation",
    "satellite and connectivity",
    "security",
    "workspaces",
    "learning systems",
    "marketplaces",
  ],
};

fs.writeFileSync(path.join(outDir, "latest-market-radar-report.json"), JSON.stringify(report, null, 2) + "\n", "utf8");
console.log("Pantavion market radar report written.");
