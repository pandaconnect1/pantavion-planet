# Supabase migration archive — pre-reconcile 2026-08-27

These SQL files were removed from the active `supabase/migrations` path during forensic reconciliation against the production `supabase_migrations.schema_migrations` ledger.

They are preserved byte-for-byte for recovery and historical analysis. They are not canonical production migrations and must not be executed automatically. Reasons include exact/normalized duplication under another production timestamp, consolidation into a later canonical migration, or replacement by a newer production model.

The original pre-reconciliation state is also preserved on branch `archive/supabase-migrations-pre-reconcile-20260827`.
