name: Pantavion Generate Foundry Fencing Migration Name

on:
  push:
    branches:
      - security/foundry-worker-fencing-20260829
    paths:
      - ".github/workflows/pantavion-generate-foundry-fencing-migration-name.yml"

permissions:
  contents: read

jobs:
  generate-name:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Generate migration name with Supabase CLI
        shell: bash
        run: |
          set -euo pipefail
          npx --yes supabase@2.115.0 migration new durable_foundry_terminal_failure_fencing
          migration="$(find supabase/migrations -maxdepth 1 -type f -name '*_durable_foundry_terminal_failure_fencing.sql' -printf '%f\n' | sort | tail -n 1)"
          test -n "$migration"
          printf 'GENERATED_MIGRATION=%s\n' "$migration"
